"""Legacy prediction API package placeholder.

File summary:
- Keeps the old `app.api.predict` import path available if anything imports it.
- Contains no active routes because prediction lives in `api/routes/predict.py`.
- Helps separate older module names from the current versioned API layout.
"""
