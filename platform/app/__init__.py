"""Platform application package.

File summary:
- Marks `app` as the importable Python package for the platform service.
- Allows modules like `app.main`, `app.services`, and `app.ml` to be imported.
- Keeps package-level setup empty so startup work stays in `app/main.py`.
"""
