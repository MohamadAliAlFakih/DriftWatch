"""Platform test package.

File summary:
- Marks platform tests as an importable Python package.
- Groups tests for API routes, ML helpers, services, schema validation, and promotion.
- Keeps test-only helpers inside the `platform/tests` area.
"""
